.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist

# Local QA captures and agent tooling — machine state, not project source
.qa-screenshots/
.agents/
skills-lock.json

# Per-machine build override (see Config/Debug.xcconfig)
Config/Local.xcconfig
